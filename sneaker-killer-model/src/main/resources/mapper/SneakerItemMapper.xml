<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.patrick.sneakerkillermodel.mapper.SneakerItemMapper">

    <select id="getById" parameterType="Integer" resultType="SneakerItem">
        select * from sneaker_item where id = #{value}
    </select>

    <select id="getNameById" parameterType="Integer" resultType="String">
        select name from sneaker_item where id = #{value}
    </select>

</mapper>